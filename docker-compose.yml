version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: desersion-predict:latest
    container_name: desersion_predict_app
    ports:
      - "5001:5000"
    environment:
      - FLASK_ENV=production
      - PORT=5000
    restart: always
